# TODO

## Status (2026-07-15)

- CUDA GA, 6DOF simulation, renderer, STL export, checkpoints, and warm starts are implemented.
- Optimization now targets a 250 mm printer volume and a 0.45 mm PLA shell instead of 80 gsm paper.
- Launch/domain randomization covers speed, height, attitude error, angular release error, and gusty wind.
- Tail-aware score uses mean, worst-quartile CVaR, and worst-case distance; champion selection uses a separate 128-scenario holdout suite.
- Five printable baseline designs and `seeds.bin` are generated by `make presets`.
- Swarm rendering compacts real triangle ranges and uses pinned frame readback.
- Host geometry/STL tests pass with `make host-test`.
- CUDA 13.1, MSVC Build Tools, and ffmpeg are working on the local RTX 3070; 4,096-scenario audits and 96-scenario path videos are validated.

## Next

1. Repeat the validated smoke build/search on the RTX 5090 host using `./build-windows.ps1 -Arch sm_120` or the Linux Makefile.
2. Compare kernel occupancy and render frame time before/after compact triangle emission.
3. Run the full 8-seed search under the existing 400 W cap.
4. Slice and throw the top 3-5 designs; record measured mass, CG, release speed/angle, wind, and distance.
5. Add calibration inputs for measured drag, PLA density, wall width, and launch distributions.
6. Add STL ingestion or parametric fitting for third-party reference aircraft.
7. Consider aeroelastic bending, layer anisotropy, and impact survivability objectives.
