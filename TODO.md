# TODO

## Status (2026-07-16)

- CUDA GA, 6DOF simulation, renderer, STL export, checkpoints, and warm starts are implemented.
- Optimization now targets a 250 mm printer volume and a 0.45 mm PLA shell instead of 80 gsm paper.
- Launch/domain randomization covers speed, height, held attitude, independent force direction, off-CG grip impulse, angular release error, and gusty wind.
- Print-domain randomization covers PLA/flow mass, equivalent infill/ribs, asymmetric CG bias, layer drag, and mild print warp.
- The 35-gene search adds symmetric cubic-Bezier/elliptical/delta planforms, tail sweep/taper/dihedral, and center/twin/triple/outboard fin structures.
- Tail-aware score emphasizes mean distance while retaining worst-quartile CVaR and worst-case pressure; the top eight candidates each generation use a separate 128-scenario holdout suite.
- Five printable baseline designs and `seeds.bin` are generated by `make presets`.
- Swarm rendering compacts real triangle ranges and uses pinned frame readback.
- Host geometry/STL and randomized printed-mass tests pass with `make host-test`.
- A 300-generation x 10,000-plane run and independent 10,000-scenario audit are saved under `out/realistic-run`.
- A second 100-generation x 100,000-plane run, chronological 24 fps evolution reel, printable champion, and 10,000-scenario audit are saved under `out/realistic-run2`.
- The no-video high-diversity run evaluated 100,000 geometries for 300 generations across 12 training environments and audited its final curved triple-fin champion over 100,000 new scenarios under `out/shape-robust-run`.
- CUDA 13.1, MSVC Build Tools, and ffmpeg are working on the local RTX 3070; 4,096-scenario audits and 96-scenario path videos are validated.

## Next

1. Repeat the validated smoke build/search on the RTX 5090 host using `./build-windows.ps1 -Arch sm_120` or the Linux Makefile.
2. Compare kernel occupancy and render frame time before/after compact triangle emission.
3. Slice and throw the top 3-5 designs; record measured mass, CG, release speed/angle, wind, and distance.
4. Add calibration inputs for measured drag, PLA density, wall width, infill pattern, and launch distributions.
5. Add STL ingestion or parametric fitting for third-party reference aircraft.
6. Consider aeroelastic bending, layer anisotropy, and impact survivability objectives.
